plugins {
    id 'java'
}

subprojects {
    plugins.apply('java')
    plugins.apply('maven-publish')

    group 'io.github.luizotavio'
    version '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

jar.enabled = false